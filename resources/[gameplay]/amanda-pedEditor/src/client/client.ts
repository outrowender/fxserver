import { startMenu } from "./src/menu"

onNet('amanda-pedEditor:start', value => {
    startMenu(null)
})

//async function startup() {
    //FreezeEntityPosition(PlayerId(), true)
//}

//setTick(startup)